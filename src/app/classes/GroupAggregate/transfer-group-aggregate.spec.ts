import { TransferGroupAggregate } from './transfer-group-aggregate';

describe('TransferGroupAggregate', () => {
  it('should create an instance', () => {
    expect(new TransferGroupAggregate()).toBeTruthy();
  });
});
