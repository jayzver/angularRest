import { ProxyGroupAggregate } from './proxy-group-aggregate';

describe('TransferGroupAggregate', () => {
  it('should create an instance', () => {
    expect(new ProxyGroupAggregate()).toBeTruthy();
  });
});
