import { AdditionalProperties } from './additional-properties';

describe('AdditionalProperties', () => {
  it('should create an instance', () => {
    expect(new AdditionalProperties()).toBeTruthy();
  });
});
